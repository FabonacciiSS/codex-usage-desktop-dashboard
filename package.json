{
  "name": "ai-usage-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Local desktop dashboard for AI usage: ChatGPT Codex, Car360, DeepSeek and OpenCode Go quotas.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "check": "node --check src/main.js && node --check src/preload.js && node --check src/renderer.js"
  },
  "devDependencies": {
    "electron": "^32.2.0"
  }
}
